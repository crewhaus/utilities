{
  "name": "@crewhaus/crewhaus-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-based REPL for the CrewHaus meta-harness compiler — Monaco editor + live trace timeline + OAuth quota + cross-tenant isolation (Section 35).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "test": "bun test src",
    "play:server": "bun src/cli.ts"
  },
  "dependencies": {
    "@crewhaus/scaffold-templates": "workspace:*"
  }
}
